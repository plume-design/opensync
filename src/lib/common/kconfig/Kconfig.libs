menu "OS Common configuration"

    config OS_EV_TRACE
    bool "enable libev event tracing"
    default n
    help
        Enable libev tracing. libev must have support for it.

    config MEM_MONITOR
    bool "dynamic memory allocation monitoring"
    default n
    help
            Enable dynamic memory allocation monitoring

endmenu