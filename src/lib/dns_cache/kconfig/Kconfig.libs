menu "libdns_cache Configuration"
    config LIBDNS_CACHE
        bool "Library for per device-ip lookup actions cache"
        default y
        help
            Enable support for caching per device per ip lookup actions cache
endmenu
